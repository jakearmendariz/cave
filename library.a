(def {fun} (\ {args body} {def (head args) (\ (tail args) body)}))

(fun{add x y} {+ x y})

(fun{var x , b , a} 
{
    def {x} a
})

(fun{sum_array arr}
{
    (def {sum} 0)
    (for {x in arr}{
        (def {sum} (+ sum x))
        (print sum)
    })
    (print "sum_array complete")
})