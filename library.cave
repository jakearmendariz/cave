(def {fun} (\ {args body} {def (head args) (\ (tail args) body)}))

(fun{add x y} {+ x y})

(fun{var x , b , a} 
{
    def {x} a
})

#Finds the sum of all elements in array
(fun{sum_array arr}
{
    (def {sum} 0)
    (for {i from 0 10}{
        (def {sum} {+ sum 1})
    })
})